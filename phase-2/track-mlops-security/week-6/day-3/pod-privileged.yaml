apiVersion: v1
kind: Pod
metadata:
  name: bad-pod
  namespace: ns-baseline
spec:
  containers:
  - name: alpine
    image: alpine
    command: ["sleep", "9999"]
    securityContext:
      privileged: true