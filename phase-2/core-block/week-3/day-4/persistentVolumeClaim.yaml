apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-dynamic
spec:
    storageClassName: dynamic-sc
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
